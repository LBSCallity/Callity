services:
  - type: web
    name: callity-api
    env: python
    buildCommand: ""
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    runtime: docker
    dockerfilePath: ./Dockerfile
